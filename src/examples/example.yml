description: >
  Run migrations and tests against an ephemeral Neon branch in CI.
usage:
  version: 2.1
  orbs:
    neon: dhanushreddy291/neon@1.0
  workflows:
    test:
      jobs:
        - neon/run_tests:
            migrate_command: npm run db:migrate
            test_command: npm test
