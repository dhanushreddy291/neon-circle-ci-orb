version: 2.1

description: >
  Neon CircleCI Orb. Provision ephemeral Postgres branches for every CI
  pipeline run, run migrations and tests against isolated databases, and
  automatically clean up when finished.

display:
  home_url: "https://neon.com"
  source_url: "https://github.com/dhanushreddy291/neon-circle-ci-orb"
